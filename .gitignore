# deps / build
node_modules/
dist/
.astro/
.vercel/
.wrangler/

# env / secrets (PostHog key, deploy tokens live here — never commit)
.env
.env.*
!.env.example

# build/preview artifacts
preview*.png

# os / editor
.DS_Store
Thumbs.db
*.log
