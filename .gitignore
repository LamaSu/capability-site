# deps / build
node_modules/
dist/
.astro/
.vercel/
.wrangler/

# env / secrets (PostHog key, deploy tokens live here — never commit)
.env
.env.*
!.env.example

# os / editor
.DS_Store
Thumbs.db
*.log
